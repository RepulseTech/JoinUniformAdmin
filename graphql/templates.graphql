query templates {
  categories {
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
    edges {
      cursor
      node {
        id
        name
        examPackTemplate {
          id
          disabled
          created
          updated
        }
      }
    }
  }
}